import { Typography } from 'antd';

const { Text } = Typography;

const Emptytext = () => {
  return <Text style={{ fontSize: '22px', fontWeight: 700 }}>No Designs Created</Text>;
};

export default Emptytext;
